const fs = require('fs');
const path = require('path');

const cardJson = JSON.parse(fs.readFileSync(path.join(__dirname,"../gameDatas/cards.json")))



class card {

  constructor(cardNumber) {//Loads info from the json

  
  
    
  }



}